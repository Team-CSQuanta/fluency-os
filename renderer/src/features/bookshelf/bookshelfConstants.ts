// Design constants, not sample data — the formats the importer actually
// accepts, mirroring the CHECK constraint on books.format and the Electron
// file-picker filter.
export const BOOK_FORMATS = ['pdf', 'epub', 'mobi', 'azw3', 'txt'];
