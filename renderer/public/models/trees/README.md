Drop converted `.glb` tree models here, named `<species>-<stage>.glb` —
species being broadleaf, conifer or shrub, and stage 0 to 5.

Anything absent falls back to the procedural low-poly trees, so this directory
being empty is a supported state, not a missing step.

See docs/forest-models.md.
